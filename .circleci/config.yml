version: 2
jobs:
  build:
    docker:
        - image: circleci/rust:1.35.0
    steps:
      - checkout
      - run: git config --global --unset "url.ssh://git@github.com.insteadof" || true
      - run: git config -l
      - run: make c-bindings